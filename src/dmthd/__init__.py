"""D-MTHD Phase-3 codebase: data preparation, teacher adaptation, teacher caching,
student distillation (fine-tune only / single-teacher / uniform / D-MTHD), evaluation,
efficiency benchmark and result aggregation.

Every entry point is a module you run with `python -m dmthd.<name> --help`.
"""
__version__ = "0.1.0"
